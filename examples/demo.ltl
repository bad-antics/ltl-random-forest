// examples/demo.ltl — runnable demo for ltl_random_forest

import std::io
use ltl_random_forest as lib

fn main() {
    io::println("ltl_random_forest demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}